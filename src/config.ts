// Dev deployment URL (matches CONVEX_URL in .env.local).
// TODO: switch per-environment (react-native-config or build flavor) before release.
export const CONVEX_URL = 'https://valuable-ostrich-597.convex.cloud';
